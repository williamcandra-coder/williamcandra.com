"""Goh Dip Tong Stage 2 — calculation and research engine.

Kept as a real package rather than a namespace one, matching ``pipeline``, so
imports resolve identically from a developer's shell, a test and a CI runner.
"""
